// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v4.24.4
// source: osmosis/poolmanager/v1beta1/swap_route.proto
/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
export var protobufPackage = "osmosis.poolmanager.v1beta1";
function createBaseSwapAmountInRoute() {
    return { poolId: "0", tokenOutDenom: "" };
}
export var SwapAmountInRoute = {
    encode: function (message, writer) {
        if (writer === void 0) { writer = _m0.Writer.create(); }
        if (message.poolId !== "0") {
            writer.uint32(8).uint64(message.poolId);
        }
        if (message.tokenOutDenom !== "") {
            writer.uint32(18).string(message.tokenOutDenom);
        }
        return writer;
    },
    decode: function (input, length) {
        var reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        var end = length === undefined ? reader.len : reader.pos + length;
        var message = createBaseSwapAmountInRoute();
        while (reader.pos < end) {
            var tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.poolId = longToString(reader.uint64());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.tokenOutDenom = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON: function (object) {
        return {
            poolId: isSet(object.poolId) ? globalThis.String(object.poolId) : "0",
            tokenOutDenom: isSet(object.tokenOutDenom) ? globalThis.String(object.tokenOutDenom) : "",
        };
    },
    toJSON: function (message) {
        var obj = {};
        if (message.poolId !== "0") {
            obj.poolId = message.poolId;
        }
        if (message.tokenOutDenom !== "") {
            obj.tokenOutDenom = message.tokenOutDenom;
        }
        return obj;
    },
    create: function (base) {
        return SwapAmountInRoute.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial: function (object) {
        var _a, _b;
        var message = createBaseSwapAmountInRoute();
        message.poolId = (_a = object.poolId) !== null && _a !== void 0 ? _a : "0";
        message.tokenOutDenom = (_b = object.tokenOutDenom) !== null && _b !== void 0 ? _b : "";
        return message;
    },
};
function createBaseSwapAmountOutRoute() {
    return { poolId: "0", tokenInDenom: "" };
}
export var SwapAmountOutRoute = {
    encode: function (message, writer) {
        if (writer === void 0) { writer = _m0.Writer.create(); }
        if (message.poolId !== "0") {
            writer.uint32(8).uint64(message.poolId);
        }
        if (message.tokenInDenom !== "") {
            writer.uint32(18).string(message.tokenInDenom);
        }
        return writer;
    },
    decode: function (input, length) {
        var reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        var end = length === undefined ? reader.len : reader.pos + length;
        var message = createBaseSwapAmountOutRoute();
        while (reader.pos < end) {
            var tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.poolId = longToString(reader.uint64());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.tokenInDenom = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON: function (object) {
        return {
            poolId: isSet(object.poolId) ? globalThis.String(object.poolId) : "0",
            tokenInDenom: isSet(object.tokenInDenom) ? globalThis.String(object.tokenInDenom) : "",
        };
    },
    toJSON: function (message) {
        var obj = {};
        if (message.poolId !== "0") {
            obj.poolId = message.poolId;
        }
        if (message.tokenInDenom !== "") {
            obj.tokenInDenom = message.tokenInDenom;
        }
        return obj;
    },
    create: function (base) {
        return SwapAmountOutRoute.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial: function (object) {
        var _a, _b;
        var message = createBaseSwapAmountOutRoute();
        message.poolId = (_a = object.poolId) !== null && _a !== void 0 ? _a : "0";
        message.tokenInDenom = (_b = object.tokenInDenom) !== null && _b !== void 0 ? _b : "";
        return message;
    },
};
function createBaseSwapAmountInSplitRoute() {
    return { pools: [], tokenInAmount: "" };
}
export var SwapAmountInSplitRoute = {
    encode: function (message, writer) {
        if (writer === void 0) { writer = _m0.Writer.create(); }
        for (var _i = 0, _a = message.pools; _i < _a.length; _i++) {
            var v = _a[_i];
            SwapAmountInRoute.encode(v, writer.uint32(10).fork()).ldelim();
        }
        if (message.tokenInAmount !== "") {
            writer.uint32(18).string(message.tokenInAmount);
        }
        return writer;
    },
    decode: function (input, length) {
        var reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        var end = length === undefined ? reader.len : reader.pos + length;
        var message = createBaseSwapAmountInSplitRoute();
        while (reader.pos < end) {
            var tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.pools.push(SwapAmountInRoute.decode(reader, reader.uint32()));
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.tokenInAmount = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON: function (object) {
        return {
            pools: globalThis.Array.isArray(object === null || object === void 0 ? void 0 : object.pools) ? object.pools.map(function (e) { return SwapAmountInRoute.fromJSON(e); }) : [],
            tokenInAmount: isSet(object.tokenInAmount) ? globalThis.String(object.tokenInAmount) : "",
        };
    },
    toJSON: function (message) {
        var _a;
        var obj = {};
        if ((_a = message.pools) === null || _a === void 0 ? void 0 : _a.length) {
            obj.pools = message.pools.map(function (e) { return SwapAmountInRoute.toJSON(e); });
        }
        if (message.tokenInAmount !== "") {
            obj.tokenInAmount = message.tokenInAmount;
        }
        return obj;
    },
    create: function (base) {
        return SwapAmountInSplitRoute.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial: function (object) {
        var _a, _b;
        var message = createBaseSwapAmountInSplitRoute();
        message.pools = ((_a = object.pools) === null || _a === void 0 ? void 0 : _a.map(function (e) { return SwapAmountInRoute.fromPartial(e); })) || [];
        message.tokenInAmount = (_b = object.tokenInAmount) !== null && _b !== void 0 ? _b : "";
        return message;
    },
};
function createBaseSwapAmountOutSplitRoute() {
    return { pools: [], tokenOutAmount: "" };
}
export var SwapAmountOutSplitRoute = {
    encode: function (message, writer) {
        if (writer === void 0) { writer = _m0.Writer.create(); }
        for (var _i = 0, _a = message.pools; _i < _a.length; _i++) {
            var v = _a[_i];
            SwapAmountOutRoute.encode(v, writer.uint32(10).fork()).ldelim();
        }
        if (message.tokenOutAmount !== "") {
            writer.uint32(18).string(message.tokenOutAmount);
        }
        return writer;
    },
    decode: function (input, length) {
        var reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        var end = length === undefined ? reader.len : reader.pos + length;
        var message = createBaseSwapAmountOutSplitRoute();
        while (reader.pos < end) {
            var tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.pools.push(SwapAmountOutRoute.decode(reader, reader.uint32()));
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.tokenOutAmount = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON: function (object) {
        return {
            pools: globalThis.Array.isArray(object === null || object === void 0 ? void 0 : object.pools)
                ? object.pools.map(function (e) { return SwapAmountOutRoute.fromJSON(e); })
                : [],
            tokenOutAmount: isSet(object.tokenOutAmount) ? globalThis.String(object.tokenOutAmount) : "",
        };
    },
    toJSON: function (message) {
        var _a;
        var obj = {};
        if ((_a = message.pools) === null || _a === void 0 ? void 0 : _a.length) {
            obj.pools = message.pools.map(function (e) { return SwapAmountOutRoute.toJSON(e); });
        }
        if (message.tokenOutAmount !== "") {
            obj.tokenOutAmount = message.tokenOutAmount;
        }
        return obj;
    },
    create: function (base) {
        return SwapAmountOutSplitRoute.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial: function (object) {
        var _a, _b;
        var message = createBaseSwapAmountOutSplitRoute();
        message.pools = ((_a = object.pools) === null || _a === void 0 ? void 0 : _a.map(function (e) { return SwapAmountOutRoute.fromPartial(e); })) || [];
        message.tokenOutAmount = (_b = object.tokenOutAmount) !== null && _b !== void 0 ? _b : "";
        return message;
    },
};
function longToString(long) {
    return long.toString();
}
if (_m0.util.Long !== Long) {
    _m0.util.Long = Long;
    _m0.configure();
}
function isSet(value) {
    return value !== null && value !== undefined;
}
